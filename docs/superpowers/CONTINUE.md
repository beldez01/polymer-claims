# ⟳ Polymer Claims — RESUME HERE

> This file is surfaced automatically by a SessionStart hook so a fresh-context session
> knows exactly where the v1.3 grammar build stands and how to continue. **Keep it current:
> update the "Current state" + "Next" lines at each phase boundary.**

## Current state (as of 2026-06-05 — live node server complete)

**✅ LIVE TIMELINE — BUILT (2026-06-05, merged `a2a8fb1` engine + `13b070b` viewer, local-only).** The viewer now plays the claims universe **evolving across `run_cycle` iterations** (the north-star "buzzing universe"). **Engine (protocol-only):** `export_topology` gained a `seed_positions` **warm-start** (existing nodes hold position frame-to-frame; `layout_id` `seed=warm`; cooler `_FR_WARM_TEMP` for bounded drift; no-seed path byte-identical) + `export_timeline(corpus, adapters, ctx, *, n_cycles) -> TopologyTimeline` (frozen `FrameStats`/`TimelineFrame`/`TopologyTimeline`; runs run_cycle threading corpus+ledger, snapshots a warm-started frame + per-cycle stats) + CLI `export-timeline` + `viewer/scripts/make_timeline.py` → `viewer/public/sample-timeline.json` (10 frames: 0→10 licensed, rival nodes at frame 1, a representation-revision at frame 6; max inter-frame node drift 0.51 = stable). **Viewer:** timeline store + a D2 **transport bar** (play/pause/scrub/speed/frame-counter) + a pure `interpolateFrame` blender (position lerp + status-color crossfade + node enter/exit + edge fade) + per-frame `FrameStats` readout. **Verified in-browser:** frame 0 (11 amber pending) → frame 6 (14 nodes, 10 blue licensed + the gray octahedron revision) with stable positions, auto-pause at the last frame. Pure/deterministic (animation time is client-side only); grammar untouched; Corpus at 4. 302 protocol + 325 grammar green, viewer tsc+build clean. Spec `docs/superpowers/specs/2026-06-05-live-timeline-design.md`, plan `…-live-timeline.md`.

**✅ LIVE NODE SERVER — BUILT (2026-06-05, merged `f59dd33` engine/Branch A + `1cec2b1` viewer/Branch B, local-only). The viewer is now TRULY live — it streams from a running node, not a precomputed file.** **Branch A (umbrella `polymer_claims` pkg only — engine pure, untouched):** `NodeRunner` (`src/polymer_claims/node.py`, the IMPURE driver — owns the loop; `from_seed(corpus, *, scheduler_budget=1e9, **run_cycle_kwargs)`; `tick()` runs the #5d `next_action` scheduler → `run_cycle` on RUN_CYCLE / idle-heartbeat otherwise → warm-started `export_topology` frame, reusing the protocol's own `_frame_stats`/`_n_licensed` so stats are byte-identical to `export_timeline`; `snapshot()->TopologyTimeline`) + a **FastAPI SSE server** (`src/polymer_claims/server.py`, `create_app(runner,*,interval,origins,autostart)`: a cancellable background ticker broadcasts each frame to per-subscriber asyncio queues; endpoints `GET /` `/state` `/timeline` + **`/stream`** (`event: frame` SSE, 15s heartbeat) + `POST /step` `/pause` `/resume`; CORS default `localhost:3000`) shipped as an **optional `[serve]` extra** (`fastapi`/`uvicorn`/`sse-starlette`; core wheel + CLI import WITHOUT it via a lazy `_import_server()`) + CLI **`serve`** (`--seed-corpus`/`--host`/`--port`/`--interval`/`--budget`/`--origins`) + `src/polymer_claims/seed.py` `default_seed_corpus()` (an EVOLVING built-in: 6 PENDING-plan claims + sentinel + `rival_generation` + a representation-revision proposer + CostModel/budget so the live universe **grows and licenses progressively** — 0→2→4→6, rival nodes 7→10, octahedron at frame 4). **KEY FIX (`dcfe990`):** renamed the scheduler-budget param to `scheduler_budget` so a `budget` in `run_cycle_kwargs` flows to run_cycle's SELECT (progressive licensing, not all-at-once). **Branch B (viewer live mode, REUSES the existing warm-started interpolation + transport bar — no new playback engine):** `viewer/src/lib/live.ts` (`connectLive(url,{onTimeline,onFrame,onStatus})` — seeds from `/timeline`, subscribes to `/stream` via `EventSource` (auto-reconnects), returns `disconnect()`); store live-mode (`liveUrl`/`connected`/`following` + `connectLive`/`disconnectLive`/`pushFrame`(append, **deduped by `cycle_index`** so the SSE on-connect repeat + reconnect replays don't double-append; glides if following)/`jumpToLive`; `seek` now clears `following` so a manual scrub stops auto-follow); `LiveControl` panel (top-right D2 hairline: URL input + Connect/Disconnect + **● LIVE** dot — filled when following / hollow ring when scrubbed, NO glow + `live · frame NN` mono status) + a **`⇥ LIVE`** jump button in the transport bar (shown only when `connected && !following`). **VERIFIED END-TO-END in Chrome** (live node + viewer): connect → frame counter advances **17→21 in real time** tracking the node; scrub-back → `frame 01/67` playback on accumulated frames + `⇥ LIVE` appears; jump-to-live → snaps to `frame 68/68` + resumes following. 18 umbrella + 302 protocol green, viewer tsc+build clean. Spec `docs/superpowers/specs/2026-06-05-live-node-server-design.md`, plan `…-live-node-server.md`.

**▶ NEXT ACTION (USER-GATED, pick one): (a) PolymerGenomicsAPI integration** — lift `<ClaimUniverse>` + `theme.ts` + the live-mode surface into `/Users/zbb2/Desktop/PolymerGenomicsAPI/viewer/` to REPLACE the obsolete `FormalClaimUniverse` on polymerbio.org (aesthetic already matches the D2 system by construction; no API-repo change made yet). **(b) PyPI publish** of `polymer-claims` (flagged account blocks OIDC/Actions — do it LOCALLY with a token, only when the user says so; the build + `[serve]` extra are ready). **(c) harden-at-scale** — M1 (`canonicalize` mints `status=LICENSED` structural equivalences with no evidential test → give them a non-LICENSED status), I2 (`grounded_extension` ~O(N³) fixpoint → worklist rewrite + ingestion size/depth bounds; only matters when accepting large untrusted corpora), I1 defense-in-depth cardinality cap. **(d) deploy-an-agent UX / BYO-compute** — the federated layer ([[project_polymer_claims_platform_vision]]): a `POST /inject` claim endpoint is a noted future hook on the live server. **NEEDS USER (unchanged):** the `strength.py` uncertainty-polarity fix is DONE (rename), but any push/publish is user-gated. F4 (accumulating-belief) found already wired.

**✅ CLAIMS-UNIVERSE VIEWER (3D front-end renderer) — BUILT (2026-06-05, merged `9f520ed`, local-only).** A standalone **Next 16 + React Three Fiber** app at `polymer-claims/viewer/` that renders the `TopologyExport` as an **ultra-precise metrological epistemic instrument** in the **current polymerbio.org D2 aesthetic** (IBM Carbon × Bloomberg: light `#F4F4F5` canvas, electric-blue `#0F62FE` SINGULAR structural accent, hairline reference frame + ground grid + **ticked axes with mono numeric labels**, **matte** nodes — NO glow/emissive/shadow/neon, tabular-nums readouts, `§0x` markers). **⚠️ AESTHETIC SOURCE = the redesigned `/Users/zbb2/Desktop/PolymerGenomicsAPI/viewer` front-end + its `docs/redesign/2026-05-19-viewer-redesign-design.md` + `src/config/theme.ts` — NOT the obsolete `FormalClaimUniverse` claims viewer (that glowy component is exactly what this REPLACES) and NOT MechanoGenomics/the landscape sub-app (both wrong guesses I was corrected on).** Encodings: status→node color (licensed=blue/pending=amber/exploratory=violet/conjectured=gray/rejected=rose), `is_representation_revision`→octahedron glyph, edge kind→color + provisional=dashed/effective=solid; click→inspector shows the exact `strength` 6-vector (tabular-nums); status/edge/provisional filters; live camera-coord readout. Sample corpus generated through the engine (`viewer/scripts/make_sample.py` → `export_topology` → `public/sample-topology.json`, 26 nodes, all variety). `npm run dev`/`build`+`tsc` clean; verified in-browser. Stack mirrors the API viewer EXACTLY so `<ClaimUniverse>` + `theme.ts` lift into `PolymerGenomicsAPI/viewer/` later. **ULTIMATE GOAL: integration into PolymerGenomicsAPI (polymerbio.org) — NOT done yet, user-gated; no API-repo changes were made.** Spec `docs/superpowers/specs/2026-06-05-claims-viewer-design.md`, plan `…-claims-viewer.md`.

**✅ MDL GATE + NOVELTY-RESIDUAL META-TIER — BUILT (2026-06-05, merged local-only).** A port of Wang & Buehler (*Self-Revising Discovery Systems for Science*, arXiv:2606.01444, 2026) into our `representation_revision` meta-tier: a representation revision now earns its license from the corpus's **own compressibility**. **Branch A grammar (`b053d01`):** new `description_length.py` — a two-part *structural* MDL code over the typed claim corpus (locked choices: structural granularity only / corpus-relative `−log2(freq)` ontology cost / Rissanen `log*` counts), `transport` as the Left-Kan analog (MERGE repoint, ADD add-atom, DEPRECATE repoint-to-generic incl. a generic-TERM sentinel; RELAX deferred), `mdl_delta`/`novelty_residual`/`clears_mdl_bar` (**policy β: gate = compression `delta<−eps`; residual only CLASSIFIES discovery vs consolidation**), `RevisionDiscovery`, + `LicenseRoute.MDL_GATE` accepted by `meets_meta_tier_bar`. Correctness-reviewed: proper prefix code (Kraft<1), exact signs/magnitudes; one real bug found+fixed (term-DEPRECATE incoherence → spurious license at T=3). **Branch B protocol (`2fce808`):** `verify_stage` tries the MDL route for representation-revisions — a compressing revision LICENSES via `MDL_GATE` end-to-end through `run_cycle` (verified: redundant-MERGE `mdl_delta −17`, ends LICENSED); non-compressing holds PENDING; ordinary claims + belief-neutrality unaffected. **The flywheel now turns on schema** (the RED-TEAM daemon's revisions self-adjudicate by compression — no human replication needed; addresses W&B's own "gates are engineering choice" open problem). 325 grammar + 294 protocol green, ruff clean, isolation holds, Corpus still 4 (`Schema` is ephemeral/derived). Spec `docs/superpowers/specs/2026-06-05-mdl-meta-tier-design.md`, plan `…-mdl-meta-tier.md`. **Deferred:** RELAX-through-MDL (needs constraint-aware code), SELECT using `novelty_residual` as a value axis, field-resolved pattern signatures, full `RevisionDiscovery` threading on `CycleResult`, the categorical (copresheaf/Kan) formalism. See [[project_tet2_precedent_swarm]]-style competitor note [[project_wang_buehler_categorical_discovery]].


**✅ SEMANTICS FINALIZATION + PRE-DEPLOY SECURITY HARDENING DONE (2026-06-05 session).** The three remaining semantics decisions were resolved by direct user choice and merged (local-only): **(1) `StrengthVector.uncertainty` → `certainty` rename** (`38f829e`) — the lattice is now uniformly higher-is-better, **the strength-polarity inconsistency is RESOLVED** (no more reverse-polarity axis; the F2 oracle special-case collapsed to a plain `meet`; `QuantityLeaf.uncertainty` + `OracleDossier.relative_uncertainty` deliberately untouched). **(2) Credit-on-survival** (`4fe6791`) — `update_ledger` moved AFTER `integrate` so a claim retracted by INTEGRATE's AGM contest earns no success credit. **(3) F4 / accumulating-belief — found ALREADY WIRED** (`select.py:49` + `economics.py:106` use `accumulated_belief`; the "dormant" note was stale); credit-on-survival makes it compound on survival. Spec `docs/superpowers/specs/2026-06-05-semantics-finalization-design.md`, plan `…-semantics-finalization.md`. **Then a comprehensive architect+security audit (4 Opus auditors + linter/security sweep) before public deploy:** linter/security sweep clean (NO eval/exec/pickle/subprocess/network/file-IO; NO clock/random; B008 hits are immutable-model false positives). Polarity audit = RENAME-COMPLETE (0 findings). Credit audit = CORRECT (0 bugs). **Security red-team found 2 deploy-blocking trust-boundary holes — both FIXED + auditor-re-verified, merged `b090b1f`:** **C1** — untrusted/proposal-supplied defeat edges could be immediately effective (non-provisional from an unlicensed source) and knock out honest LICENSED claims; fix = `bridge_proposer` coerces edges to provisional + `compile_to_IR` rejects ANY non-provisional proposal-supplied edge (incl. the synthetic `:`-source carve-out the auditor caught on re-verify). **C2** — `injected=`/`proposers=` ports didn't enforce the licensing invariant; fix = `compile_to_IR` rejects any proposal arriving LICENSED or carrying a licensing block, on every port ("licensing minted only by verify", safe-by-construction). **I1 (BH-bar cardinality inflation) found NOT exploitable** through `run_cycle` (`select.py:149` re-stamps `search_cardinality` on every executed claim). **304 grammar + 277 protocol green; ruff clean; isolation holds.** **Tracked follow-ups (user chose blocking-only scope):** M1 — `canonicalize` mints `status=LICENSED` structural equivalences without an evidential test (give them a non-LICENSED status); I2 — `grounded_extension` fixpoint ~O(N³) on adversarial attack-chains (worklist rewrite + ingestion size/depth bounds, only matters when accepting large untrusted corpora); I1 defense-in-depth cardinality cap (optional). **▶ VIEWER SLICE + PACKAGING HARNESS — BUILT (2026-06-05, merged local-only):** **(viewer data contract `4b64570`)** pure `export_topology(corpus, *, layout) -> TopologyExport` in `polymer_protocol` (`topology.py`) — frozen nodes/edges/clusters + a deterministic Fruchterman-Reingold 3D layout seeded from id-hashes (no clock/random); edges carry effective/provisional flags computed as `represent` does; +15 tests. The live 3D renderer is the SEPARATE viewer instance (consumes this export). **(packaging `3dad729`)** option-B umbrella `polymer-claims` distribution at the repo root (`pyproject.toml` + `src/polymer_claims/{__init__,cli,io}.py`) — a thin wheel depending on `polymer-grammar`+`polymer-protocol` + a CLI (`version`/`validate`/`run-cycle`/`loop`/`export-topology`) over the COMPLETE runtime; `loop` drives the #5d `next_action` scheduler; `scripts/build_and_test_install.sh` builds all three wheels + installs into a throwaway venv + smokes the console script end-to-end (licenses ≥1 claim from an installed wheel). Two dev workflows + isolation intact; wheel hygiene publish-ready. **`pip install polymer-claims` → local node WORKS.** **304 grammar + 292 protocol + 6 umbrella green.** **▶ REMAINING (all USER-GATED):** the **PyPI publish** (blocked — flagged account; build is ready, do it locally with a token only when the user says so); the tracked security/perf follow-ups **M1** (canonicalize LICENSED-equivalence status), **I2** (grounded_extension worklist rewrite + ingestion bounds — adversarial-corpus DoS), **I1** defense-in-depth cardinality cap; the live 3D viewer + federated/compute layer (separate instance, [[project_polymer_claims_platform_vision]]).

**🏁 SUB-PROJECT #5d (loop-economics) is DONE — THE 5-SUB-PROJECT PROTOCOL RUNTIME IS COMPLETE** — branch `feat/loop-economics-5d`, merge SHA `7e1d5c9`. The final slice. New module `protocol/economics.py`: a **pure recommend-only scheduler** `next_action(state, *, budget, config) -> ScheduledAction | None` that value-ranks the next runtime action (`RUN_CYCLE` vs the three daemon passes `DRIFT`/`ORACLE_VALIDATION`/`RED_TEAM`) under a **single shared compute budget**, and recommends it for the caller to execute + loop (**recommend-only — NO `run_until` driver**, purity). **RUN_CYCLE value reuses the REAL belief-EIG** (`expected_information_gain(accumulated_belief(c, ledger))` summed over selectable PENDING+plan claims) **+ the REAL CostModel** (`aggregate_cost(cost_model.resolve(id), weights)`); the daemons use count-scaled maintenance value (n_licensed / probes / un-red-teamed) + a flat per-pass `daemon_cost`, each behind a **symmetric caller-supplied availability gate** (`current` / `probes_available` / `red_team_enabled`). `next_action` filters to `cost ≤ budget`, returns the max-value action (deterministic tie-break preferring RUN_CYCLE), else `None` (stop). **Pure/deterministic; never mutates `state`; grammar untouched; Corpus at 4.** `SchedulerState`/`SchedulerWeights`/`SchedulerConfig`/`ScheduledAction`/`ActionKind` + `next_action` exported. **3 forks self-resolved on roadmap recommendations (autonomous-overnight authorization):** E1 value-ranked, E2 recommend-only, E3 single shared budget. **Build surfaced + recorded a real runtime property:** the #3a cardinality-scaled BH bar blocks multi-claim licensing at moderate evidence (the loop integration test uses strong-evidence claims so a multi-claim cycle still licenses — documented in the spec Build note; NOT a scheduler bug). Daemon default weights calibrated to 0.01 (below the ~0.06-bit EIG-per-claim scale) so productive work dominates by default; RED_TEAM gated by `red_team_enabled` (symmetric with the other daemons — a build correction). **260 protocol + 299 grammar = 559 tests green; ruff clean; isolation holds. Opus final review = APPROVE (clean at every severity; purity verified by byte-identical state dump; the strong-evidence loop strength confirmed legitimate, fails loudly otherwise).** Spec `docs/superpowers/specs/2026-06-05-loop-economics-design.md`, plan `docs/superpowers/plans/2026-06-05-loop-economics.md`.

**Sub-project #5c (REPRESENTATION RED-TEAM daemon — third daemon slice) is DONE — ALL 3 DAEMONS COMPLETE** — branch `feat/representation-red-team-5c`, merge SHA `9996e49`. **The red-team attacks the corpus's REPRESENTATION and proposes a fix.** Two protocol-side pieces, **grammar untouched, Corpus at 4**: **(1) `protocol/red_team.py` — `RepresentationRedTeamAdapter`** — a deterministic in-package reference `GenerationAdapter` (the `TemplateGenerationAdapter` analog; identity=`representation-red-team`) that, per corpus claim, proposes ONE CONJECTURED claim carrying a `RepresentationRevision` (placeholder DEPRECATE of the claim's pattern). **Reuses the #4b-3 bus entirely (`compile_untrusted`/`bridge_proposer`) — ZERO new bus machinery** (a representation-revision is just a CONJECTURED claim carrying the field, so it rides the existing seam; real LLM red-teamers implement the same Protocol + inject). **Belief-neutral** (isolated CONJECTURED nodes, no edges — the #4a lesson) + **converges** (two skip-guards: `gen-rt-` prefix AND `representation_revision is not None`, so injected red-team outputs aren't re-red-teamed either; content-addressed ids → dup-discard). **(2) the conservative meta-tier GATE in `verify_stage`** — `if is_representation_revision(c) and not meets_meta_tier_bar(licensing): hold PENDING (continue)`. Because the auto-assembled licensing is `Licensing(SEVERE_TEST, OPEN_ACKNOWLEDGED)` (which FAILS the bar = REPLICATION + closed rival set), **every representation-revision is held back from the ordinary auto-license path** — it stays PENDING (NOT rejected) until replication-grade licensing is supplied. This enforces "schema changes are gated more conservatively" (the deferred meta-tier-slice gate). A future REPLICATION+ENUMERATED licensing WOULD pass the gate (conservative hold, not a hard block). **2 resolved forks (user-chosen):** full propose-revisions adapter (not a flag-only stub — the meta-tier unblocks it); the gate lives in #5c (verify_stage guard), not #5d. **Scope fences:** the reference adapter ships NO real intelligence (placeholder; real detection injects); NO replication-licensing path built (revisions correctly remain PENDING); NO `run_cycle` signature change (the adapter plugs into the existing `proposers=` port). **Belief-neutrality + convergence verified THROUGH `run_cycle`; pure/deterministic. 299 grammar + 247 protocol = 546 tests green; ruff clean; isolation holds. Opus final review = APPROVE (clean at every severity; the gate proven non-vacuous by counterfactual — disabling it flips the held claim to LICENSED).** (Build note: my plan's forged-licensing test forged a CONJECTURED+licensing INVALID state, which `Proposal` re-validation rejects before the bus — the implementer caught it; fix = forge a VALID LICENSED+licensing claim, the realistic injection, still dropped by `compile_untrusted`.) Spec `docs/superpowers/specs/2026-06-04-representation-red-team-design.md`, plan `docs/superpowers/plans/2026-06-04-representation-red-team.md`.

## Current state (earlier slices)

**Grammar `representation_revision` meta-tier (§5.5 — the LAST Phase-7 grammar item) is DONE — PHASE 7 IS NOW COMPLETE** — branch `feat/representation-revision-meta-tier`, merge SHA `385c45e`. **Schema/representation changes are now first-class licensable claims** (the prerequisite for #5c RED-TEAM). **Pure grammar slice — protocol untouched, Corpus at 4.** New module `grammar/representation.py`: **(1) `RepresentationRevision`** — `RevisionOperation` {ADD, DEPRECATE, MERGE, RELAX} × a **discriminated `RevisionTarget`** (`PatternTarget(patterns)` / `OntologyTermTarget(term_id)` / `ConstraintTarget(name)`, `Annotated[…, Field(discriminator="kind")]` per the `subject.py` idiom) + required `rationale` + optional `proposed_definition` (prose/JSON, NOT executable), with a `model_validator` pinning legal (operation, target) pairs (MERGE→PatternTarget≥2; RELAX→ConstraintTarget; ADD/DEPRECATE→PatternTarget-of-exactly-1 or OntologyTermTarget; never a constraint). **(2) the conservative bar (grammar represents, protocol decides):** pure `meets_meta_tier_bar(licensing)` = `route==REPLICATION AND rival_set_closure ∈ {ENUMERATED, ONTOLOGY_BOUNDED}` (+ constants `META_TIER_REQUIRED_ROUTE`/`META_TIER_ALLOWED_CLOSURES`) + `is_representation_revision(claim)` (Claim imported under `TYPE_CHECKING` — no cycle). **(3) wired as one additive-optional `Claim.representation_revision: RepresentationRevision | None = None`** — a claim CARRYING it IS a meta-tier claim, reusing ALL Claim machinery (status lifecycle, licensing bridge, provenance, defeat graph, AGM); no status restriction (the meta-ness is the field, the conservatism is the bar). **2 resolved forks (both user-chosen):** additive Claim field (not a separate top-level model); grammar-predicate/protocol-decides (not a grammar hard-validator). **Scope fences:** functorial migration (§7 Spivak Δ/Σ/Π) DEFERRED; `schema_version` frozen-as-interpreted pinning OUT; gate ENFORCEMENT is a protocol concern (this slice ships the predicate, gates nothing — the protocol slice that runs the meta-tier gate consumes `is_representation_revision` + `meets_meta_tier_bar`, likely alongside #5c/#5d); no reserved meta-pattern forced (the `Claim.pattern` slot stays unconstrained for a meta-claim — noted follow-up). **299 grammar + 235 protocol = 534 tests green; ruff clean; isolation holds; protocol untouched. Opus final review = APPROVE (clean at every severity — the empty-patterns concern is closed by the `!=1`/`>=2` guards; nested discriminated round-trip pinned for every target subtype at Claim level).** Spec `docs/superpowers/specs/2026-06-04-representation-revision-meta-tier-design.md`, plan `docs/superpowers/plans/2026-06-04-representation-revision-meta-tier.md`.

**Sub-project #5b (ORACLE-VALIDATION daemon + F2 fix — second daemon slice) is DONE** — branch `feat/oracle-validation-5b`, merge SHA `ea517c7`. **The standing D2 daemon deferred from #2**: an oracle that starts failing weakens everything it underwrites. **Touches both packages.** **Grammar `oracle.py`:** **(1) `decay_tier(tier, pass_rate) -> ValidationTier`** — `target_rank = floor(clamp(pass_rate,0,1)·MAX_TIER_RANK=4)` (via `int()`); `new_rank = min(current_rank, target_rank)`. **Proportional + decay-only + stable fixed point** (a 90%-passing oracle settles at ANCHORED and STAYS — the `min` prevents both promotion and a death-spiral; 50%→BENCHMARKED, 0%→UNVALIDATED, 100%→no cap); parameter-free, monotone. **(2) the F2 fix** (a behavior change to the shared `tier_ceiling`/`cap_strength`): `uncertainty` is **reverse-polarity** (high=weaker — confirmed `belief.py:32` `kappa=…·(1−s.uncertainty)`), so the cap now splits the empirical axes — the 3 GOODNESS axes (`magnitude`/`evidence_against_null`/`world_contact`) cap DOWN to ceiling `c` via `meet`, while `uncertainty` is floored UP to `1−c` (`_EMPIRICAL_AXES`→`_GOODNESS_EMPIRICAL_AXES`). A weak apparatus now makes a claim MORE uncertain, not less. **Protocol `oracle_validation.py` (new module, sibling to `drift.py`):** `oracle_validation_pass(registry, *, probes) -> (OracleRegistry, OracleValidationRecord)` runs passed-in `SpotProbe`s (each carries its `passed: bool` — the oracle ran OUTSIDE the pure core, like adapters), computes per-oracle pass-rate, applies `decay_tier`, and returns a **threaded registry-delta** (new `OracleRegistry` with decayed dossiers; input never mutated; **same object back when nothing changed** incl. empty/all-unknown probes) + a record (`OracleDecay` per probed registry oracle, `unknown_oracle_ids` for probes naming absent oracles — recorded but inert). **3 resolved forks:** O2 threaded registry-delta (not recompute-from-history); O1 proportional-to-pass-rate (not one-rung); F2 scoped to the cap only. **The decayed registry, fed to the next `run_cycle(..., oracles=registry')`, tightens the existing #2 `oracle_cap` seam in `verify_stage` automatically — NO new wiring** (end-to-end test pins it: decay GOLD→UNVALIDATED → `oracle_cap` drops magnitude AND raises uncertainty). **Scope fences:** decay-only (no auto-promotion — re-earning a tier needs a fresh dossier); verifier-authority decay DEFERRED (oracle tier only); F2 scoped to the cap — **`dominates`/`licensed`/`meet`/`join` in `strength.py` deliberately UNTOUCHED** (they still treat uncertainty higher=better; that broader Pareto-order/licensing-gate inconsistency is a separate strength-semantics pass, big blast radius); no `run_cycle` wiring (=#5d). **Pure/deterministic/caller-scheduled; Corpus stays 4 collections; isolation holds. 277 grammar + 235 protocol = 512 tests green; ruff clean. Opus final review = APPROVE (clean at every severity — F2 verified correct at every tier boundary).** **F2 (latent audit finding) is now FIXED.** Spec `docs/superpowers/specs/2026-06-04-oracle-validation-design.md`, plan `docs/superpowers/plans/2026-06-04-oracle-validation.md`.

**Sub-project #5a (DRIFT daemon — first daemon slice) is DONE** — branch `feat/drift-daemon-5a`, merge SHA `ce107b9`. The first STANDING process: re-examine LICENSED claims as the world's materialization context moves. **One additive grammar enum** (`PendingReason.MATERIALIZATION_DRIFTED`, the 10th — mirrors `ONTOLOGY_TERM_OBSOLETE`, a "world moved underneath the claim" reason) + new pure protocol module `drift.py`. **Two resolved forks → a clean detect/act split:** **(1) `drift_pass(corpus, *, current: MaterializationContext) -> (Corpus, DriftRecord)` is FLAG-ONLY** — scans LICENSED claims, flags those whose minted `licensing.satisfactions[*].materialization` no longer matches the passed-in `current` (freshness = ANY satisfaction matches on `api_version` AND `data_version`; **version equality**, no semver — D3 fence), returns the corpus **identity-unchanged** (`out is corpus`) + a `DriftRecord{current, examined, drifted: tuple[DriftFinding{claim_id, re_executable, licensed_versions}]}`. A LICENSED claim with `licensing=None` is counted in `examined` but never flagged. **(2) `reopen_drifted(corpus, record, *, require_plan=True) -> Corpus`** is the SEPARATE opt-in action `drift_pass` never calls itself — re-opens drifted claims to PENDING via ONE atomic `model_copy(update={status→PENDING, licensing→None, pending_reason→MATERIALIZATION_DRIFTED})` (satisfies both `Claim` validators at once), **plan-gated** (default skips planless findings — a planless re-open could never self-relicense, would strand) and **status-guarded** (skips any target no longer LICENSED — robust to a stale record; final-review hardening). Pure/deterministic/caller-scheduled (the standing #5 invariant: NO threads/clock/Date/random — `current` is an argument); daemon state lives in the returned `DriftRecord`, **Corpus stays 4 collections**; isolation holds. **Scope fences:** NO oracle-tier consultation (=#5b), NO `run_cycle` wiring (=#5d — when to run DRIFT + whether to auto-reopen is loop-economics). Prior-licensing versions survive only in `DriftRecord.licensed_versions` (grammar forbids `licensing` on non-LICENSED). **268 grammar + 18 drift tests; 496 total green; ruff clean.** Spec `docs/superpowers/specs/2026-06-04-drift-daemon-design.md`, plan `docs/superpowers/plans/2026-06-04-drift-daemon.md`.

**Protocol sub-project #4b slice-3 (intelligent-operator seam) is DONE — #4b COMPLETE** — branch `feat/intelligent-operator-seam-4b`, merge SHA `7c7a953`. Establishes the boundary where injected LLM/embedding generators attach to the bus, **zero grammar changes** (pure protocol, Corpus still 4 collections). New module `generation_adapter.py`: **(1) `GenerationAdapter` Protocol** (`identity: str` + `propose(corpus, frontier) -> tuple[Proposal,...]`) — mirrors the Phase-8 eval `Adapter`; real LLM/embedding operators implement it OUTSIDE the package. **(2) `compile_untrusted(claim, identity, *, fingerprint) -> (Claim|None, str|None)`** — the guardrail enforcing the load-bearing safety property **external generation can PROPOSE but never LICENSE**: reject a claim carrying a `licensing` block (`"untrusted-licensing"`) or whose status is LICENSED/REJECTED/PENDING-without-plan (`"untrusted-status"`); allow CONJECTURED or PENDING+plan; **force** `Provenance(AGENT_GENERATED, agent_id=identity, …)` (untrusted output can't spoof provenance or impersonate another operator); preserve declared `governance` so the existing `safety_gate` stage routes hazardous ones. **(3) `bridge_proposer(adapters) -> Proposer`** — wraps adapters onto the existing bus (FORCES `operator_id=adapter.identity`, runs `compile_untrusted`, drops rejected), so **no `run_cycle` change** and slice-2's credit economy governs each external adapter by its identity for free. **(4) `TemplateGenerationAdapter`** (identity=`template-ref`) — pure deterministic in-package reference (the `IdentityAdapter` analog) proposing one CONJECTURED elaboration per claim, skipping its own `gen-tmpl-*` outputs (converges). End-to-end pinned: the seam folds in governed conjectures **belief-neutrally**, and a cheating adapter's forged LICENSED claim **never enters the corpus**. **3 load-bearing decisions:** (1) generic `GenerationAdapter` seam (ONE boundary for embedding AND LLM — they're just external adapter identities); (2) full `compile_untrusted` guardrail anchored on propose-not-license; (3) executable frontier-attack defense DEFERRED to injected adapters (it needs hypothesis synthesis + data-ref rebinding, not a pure transplant — the honest #4b limit shrinks to "pure in-package frontier seeds stay dormant; executable defense arrives with an injected adapter"). Bridge-internal rejections aren't in the cycle `GenerationRecord` (the Proposer seam returns no record) — `compile_untrusted` is independently unit-tested for every path. **268 grammar + 208 protocol tests; ruff clean; isolation holds.** Spec `docs/superpowers/specs/2026-06-04-intelligent-operator-seam-design.md`, plan `docs/superpowers/plans/2026-06-04-intelligent-operator-seam.md`.

**Protocol sub-project #4b slice-2 (executable rivals + operator credit economy) is DONE** — branch `feat/executable-rivals-4b`, merge SHA `8e0bba0`. **Makes GENERATE self-driving.** Two coupled enrichments, **zero grammar changes** (pure protocol, Corpus still 4 collections): **(2a) executable-generation** — new pure `plan_synthesis.py` (`mirror_criterion` flips a `SatisfactionCriterion`'s comparator to its logical complement `LT↔GE/LE↔GT/EQ↔NE`, `None` for WITHIN_TOL; `transplant_plan` reuses the source `ComputeGraph` verbatim + that mirrored criterion). `rival_generation` now emits a rival of a *planned* source as a **PENDING/UNTESTED claim carrying the transplanted plan** (a real SELECT candidate) instead of an isolated node — so source + rival are a **co-evaluated adjudicator pair** (same data, exactly one SATISFIED); when the rival wins, slice-1's provisional `R⊣C` edge **autonomously activates and defeats the source** — the flywheel turns with NO injection (pinned end-to-end: a planned `c` whose data refutes it → the generated mirrored rival licenses → `c` leaves the grounded extension). Planless / WITHIN_TOL sources keep today's CONJECTURED rival (honest limit shrinks: frontier-attack seeds still dormant → slice-3). **(2b) credit economy** — new pure `allocate.py` (`allocate_subcaps`): `generate_stage` splits its global `cap` into per-operator sub-caps by #3b's `credit_factor` (above-floor operators share proportionally via largest-remainder; a below-floor operator is throttled to ONE recoverable **probation** slot, never killed; exogenous/injected EXEMPT). Discards carry reason `"operator-cap"`. Gated by one new `run_cycle(..., generation_credit_floor: float|None=None)` knob (`CREDIT_FLOOR_DEFAULT=0.5`); GENERATE runs first so it reads **last cycle's** credit. OFF by default (`None` ⇒ exact #4a flat cap). The three load-bearing #4b-slice-2 decisions: (1) executable-generation only for rivals of planned non-WITHIN_TOL sources (the one place a *pure* transplant is well-posed); (2) the mirror is the single logical complement = "not C", so a positive source's `negative` AND `null` rivals share it (accepted coarseness — a finer per-direction criterion needs richer synthesis); (3) credit economy = proportional + floor + **probation** (self-healing, no absorbing death-state), one float knob, prior-cycle ledger governs this cycle. **Belief-neutral at plant** (rival is PENDING not LICENSED ⇒ provisional edge inert; verified through `run_cycle`). **191 protocol tests (268 grammar unchanged); ruff clean; isolation holds.** Review deviation (sound): the operator-cap gate sits BEFORE the global-cap check (a throttled proposal is discarded without burning a global slot; the global cap still bounds the total since the gate only discards). Spec `docs/superpowers/specs/2026-06-04-executable-rivals-credit-economy-design.md`, plan `docs/superpowers/plans/2026-06-04-executable-rivals-credit-economy.md`.

**Protocol sub-project #4b slice-1 (provisional links — activate-on-license defeat edges) is DONE** — branch `feat/provisional-links-4b`, merge SHA `64b8042`. **The FIRST slice to touch BOTH packages.** A `DefeatEdge` can now be **provisional** (`provisional: bool = False`, additive) — inert until its source claim is LICENSED, then effective. `effective_defeats`/`grounded_extension` gain `licensed_ids: frozenset[str] = frozenset()` and skip any provisional edge whose source ∉ `licensed_ids`; the grammar AGM recompute `_in_set` and the protocol `represent` both compute + supply the LICENSED-id set (consistent activation across the cycle). GENERATE's `frontier_attack` and `rival_generation` now plant a **provisional rebut edge** (source = new claim, target = the rebutted origin, kind=REBUT, provisional=True) instead of an isolated node — belief-neutral while the seed/rival is CONJECTURED (the edge is inert under the default empty `licensed_ids`), wiring a real defeat the instant the claim validates. Closes the #4a "isolated nodes only" limitation. The four load-bearing #4b decisions: (1) **grammar flag, NOT a protocol registry** — intrinsic activate-on-license, automatic deactivation (source no longer LICENSED ⇒ inert again, no stale edge to clean), no materialization stage; (2) `licensed_ids` defaults empty so the change is **fully back-compat** (every existing edge is `provisional=False`, no existing call site changed); (3) operators use a **rebut edge, never `incompatible_with`** so `restore_consistency._conflicts` never fires — no premature retraction while conjectured; (4) honest limit: the pure operators emit **no-plan** CONJECTURED seeds, which cannot license on their own, so their *own* provisional edges stay dormant until the seed/rival gains an `evaluation_plan` (executable-generation deferred). Belief-neutrality + convergence verified through `run_cycle`; end-to-end activation pinned with a *planned* claim. **Zero new modules, Corpus still 4 collections. 268 grammar + 169 protocol tests green; isolation holds.** Spec `docs/superpowers/specs/2026-06-04-provisional-links-design.md`, plan `docs/superpowers/plans/2026-06-04-provisional-links.md`.

**Protocol sub-project #4a (GENERATE — proposer bus, pure core) is DONE** — branch `feat/generate-proposer-bus-4a`, merge SHA `5d7899f`. A new GENERATE stage in `run_cycle` (right after REPRESENT) replaces "claims enter by stuffing the Corpus": a bus of passed-in pure proposers plus an exogenous injection port — `run_cycle(..., proposers=, injected=, generation_cap=)`. Proposals run through `compile_to_IR` (discards: duplicate id, invalid edge source, unresolved edge target — logged in an ephemeral `GenerationRecord`); survivors fold into the corpus. Two PURE operators ship: `rival_generation` (CONJECTURED direction-flipped alternative-hypothesis rivals, emitted as ISOLATED nodes — no incompatible_with edge, because a conjectured claim cannot encode incompatibility without tripping restore_consistency's status-blind _conflicts; linkage deferred to #4b) and `frontier_attack` (a CONJECTURED defense SEED at each unresolved-frontier node, NO defeat edge). Both are strictly BELIEF-NEUTRAL: they add only CONJECTURED claims (no defeat edges), so the grounded extension of existing claims is unchanged — generation proposes, only EXECUTE/VERIFY decides. Content-addressed ids + a skip-own-output guard keep the corpus convergent (a second cycle adds nothing). Injected PENDING-with-plan claims license SAME cycle (scaffolding recomputed post-admission); pure CONJECTURED proposals are inert this cycle, first act next. **Zero grammar changes. 128 protocol tests green.** The seven load-bearing #4a decisions: (1) pure proposer-bus core, intelligent (embedding/LLM) operators behind the seam; (2) two pure belief-neutral operators (rival-generation + frontier-attack); (3) frontier-attack adds NO edge — belief-neutrality is FORCED because this spine's explicit edges are always effective (a strengthless conjecture's attack is not filtered, so an edge would change the grounded extension) — the defeat is derived post-license; (4) the exogenous port is the real validated entry path — injected executable claims license same-cycle via a post-admission scaffolding recompute; (5) `compile_to_IR` + discard log produced now, mined later; (6) content-addressed ids + skip-own-output convergence; (7) zero grammar changes. **#4b follow-up:** a provisional/activate-on-license edge mechanism would let frontier-attack wire a real (but inert-until-validated) defeat. Embedding/LLM operators + the per-operator credit ledger are deferred to #4b; operator-5's representation-revision lane is grammar-blocked (needs the `representation_revision` meta-tier). Spec/plan under `docs/superpowers/`.

**Grammar** (`grammar/`, `polymer_grammar`) is complete through all 8 phases. **Protocol runtime sub-project #1 (Corpus + assessment spine) is DONE** — new sibling package `polymer_protocol` (`protocol/`), one-way dep on `polymer_grammar` (isolation-tested), builds the frozen `Corpus` + seven pure stages (`represent / canonicalize / safety_gate / commit / execute_ground / verify_stage / integrate`) wired into a deterministic `run_cycle`; EXECUTE reuses the Phase-8 air-gapped `verify()`; GENERATE/SELECT stubbed as open ports. **49 protocol tests green; ruff clean; isolation holds.** Spec `docs/superpowers/specs/2026-06-02-protocol-spine-design.md`, plan `docs/superpowers/plans/2026-06-02-protocol-spine.md`. Merge commit: `c8b7279` (no-ff to main).

**Protocol sub-project #2 (Oracle dossier / D2) is DONE** — grammar `oracle.py` (OracleDossier IR + ValidationTier ladder UNVALIDATED→GOLD + tier→empirical-axis strength cap via meet + ApplicabilityDomain) + protocol `oracle.py` (OracleRegistry + oracle_cap) wired into verify_stage's LICENSED seam and run_cycle (`oracles=`). The oracle tier caps a licensed claim's EMPIRICAL strength axes (magnitude/uncertainty/evidence_against_null/world_contact; severity+explanatory_virtue untouched); unresolved/out-of-domain → effective UNVALIDATED; **always-on (no-registry = empty registry, NOT disabled)**; no new status/grammar field; builtin-only claims unaffected. Tier is about the APPARATUS not claim novelty (a novel claim on a validated oracle licenses at full strength). Spec `docs/superpowers/specs/2026-06-02-oracle-dossier-design.md`, plan `docs/superpowers/plans/2026-06-02-oracle-dossier.md`. Merge commit: `a61d7dd` (no-ff to main).

**Protocol sub-project #3a (SELECT — pursuit engine, structural core) is DONE** — branch `feat/select-pursuit-engine-3a`, merge SHA `03ae863`. A new SELECT stage in `run_cycle` REPLACES the old dumb execute-all driver: it ranks eligible PENDING claims on a two-axis value `(expected-information-gain, stakes)` under a structured, passed-in cost and a budget (`run_cycle(..., cost_model=, budget=)`), executing ONLY the selected subset; unselected claims stay PENDING for a later cycle. EIG comes from a minimal Beta–Bernoulli posterior derived from each claim's `StrengthVector` (deterministic quadrature, no embeddings); stakes = size of the claim's forward dependency cone. The search cardinality of each selection is recorded on selected claims and **tightens VERIFY's significance bar** — a cardinality-scaled Benjamini–Hochberg selective-inference correction — as the competed pool grows (identity at cardinality 1; strength-None claims exempt). Entirely protocol-side, **zero grammar changes**. **101 protocol tests green.** The five load-bearing #3a decisions: (1) minimal Beta–Bernoulli posterior → a real EIG (no embeddings); (2) structural stakes from the forward dependency cone; (3) passed-in structured cost config, NOT grammar IR; (4) Pareto-front value `(EIG, stakes)` + explicit-scalarization budget knapsack replacing the dumb driver; (5) cardinality-scaled BH VERIFY bar with M=1-identity + strength-None exemptions. **#3b follow-up surfaced in review:** the EIG quadrature degrades for very concentrated Betas (alpha+beta in the thousands) — fine for #3a since `prior_belief` caps kappa at 20, but the persisted accumulating-belief #3b path MUST guard it. **Deferred to #3b:** quality-diversity portfolios, a protected-minority/heterodox reserve lane, a surprise-Goodhart guard, and persisted cross-cycle accumulating belief.

**Protocol sub-project #3b (SELECT — QD / heterodox / Goodhart / accumulating belief) is DONE** — branch `feat/select-qd-belief-3b`, merge SHA `4293faf`. Four SELECT-hardening features on a threaded `SelectionLedger` (passed into `run_cycle(..., ledger=)` and returned on `CycleResult` — Corpus stays grammar-IR-only / 4 collections, zero grammar changes): (1) **accumulating belief** — the #3a per-cycle Beta posterior now ACCUMULATES per-claim realized outcomes across cycles (prior + outcomes), with a **settled-concentration EIG guard** (alpha+beta ≥ 200 → EIG 0) that closes the #3a quadrature follow-up; (2) **surprise-Goodhart** — a per-operator credit factor (optimistic smoothing) discounts the fill-order DENSITY of proposers whose high-EIG claims fail to ground (the Pareto front + the EIG axis stay RAW); (3) **quality-diversity** — structural cells `(pattern id, subject kind)` with per-cell budget caps spread the budget across niches; (4) **heterodox reserve lane** — a reserved budget fraction pursues dominated (off-front) candidates the main lane would never pick. Defaults are features-OFF (`reserve_fraction=0.0`, `cell_cap_fraction=1.0`, `ledger=None`) for exact #3a back-compat; recommended production values `0.2`/`0.5` passed explicitly. **Zero grammar changes. 156 protocol tests green.** The seven load-bearing #3b decisions: (1) cross-cycle state in a threaded `SelectionLedger`, NOT a 5th Corpus collection (Corpus stays grammar-IR-only); (2) accumulating belief (prior + outcomes) + settled-concentration EIG guard (closes the #3a quadrature follow-up); (3) surprise-Goodhart credit discounts *fill-order density* (optimistic smoothing → exact #3a back-compat; Pareto front + high-EIG signal stay raw); (4) QD structural cells + per-cell caps; (5) heterodox reserve lane for dominated candidates; (6) features OFF by default for back-compat (recommended `0.2`/`0.5` passed explicitly); (7) zero grammar changes. The surprise-Goodhart **per-operator ledger now EXISTS** — #4b's credit-economy can build on it.

Phase 7 (protocol-imposed fields) had #1 (provenance), #3 (governance),
#4 (online-FDR), #6 (reinterpret) + the `Claim.subject` slot done; #2 (oracle) is now UNBLOCKED
by Phase 8 (the `OperationNode.oracle_ref` slot exists). **#5 (representation_revision meta-tier)
is now DONE (`385c45e`) — PHASE 7 IS COMPLETE.** **299 grammar tests, green; ruff clean; isolation holds.**

- ✅ Phase 1 — foundation: L0 sum-typed leaf, status lifecycle, 6-axis Pareto strength, pattern registry, claim skeleton + deep immutability
- ✅ Phase 2 — L1: molecular Proposition + defeasible Equivalence (identity = licensed equivalence, never a hash)
- ✅ Phase 3 — L2: licensing bridge ((σ,M) satisfaction, severe-test|replication route, required rival_set_closure)
- ✅ Phase 4 — typed causal roles (derived/un-authorable adjustment set; Table-2 guard) + Dimension units algebra
- ✅ Phase 5 — L3: VAF defeat graph (`defeat.py`) + Duhem blame-sets (`blame.py`). Strength-mediated effective defeat (attack defeats unless target `≥`-dominates source); PTIME grounded extension over the SINGLE effective-defeat relation; opt-in `derived_rebut_edges` from L1 `incompatible_with`; L2 failed-satisfaction → `undermine` adapter; additive `equivalence.grounded_in` path replacing the LICENSED-only "IN" stub; `aggregate_blame` set algebra (intersection→robustly-blamed / difference→PENDING `duhem_underdetermined`). Merge `1cb0b88`. Spec `specs/2026-06-01-L3-defeat-and-blame-spec.md`, plan `plans/2026-06-01-L3-defeat-and-blame.md`.
- ✅ Phase 7 (#4 of 6) — online-FDR ledger (`fdr.py`). Corpus-level immutable IR entity controlling false-discovery rate over an open-ended test stream via **LOND** (`α_t = target·γ_t·(D_{t-1}+1)`, `γ_j = (6/π²)/j²`). `FDRTest`/`FDRLedger` frozen models; `process_test`/`process_stream` (append-only) + `is_discovery` query. Standalone — grammar computes the allocation, evaluator supplies p-values; no `Claim` coupling. Merge `f41375b`. Spec `specs/2026-06-01-fdr-ledger-spec.md`, plan `plans/2026-06-01-fdr-ledger.md`. (Protocol requirement #6 `reinterpret` was done in L3.)
- ✅ Phase 7 (#1 + #3) — **`Provenance`** (`provenance.py`) + **`Governance`** (`governance.py`), two additive-optional `Claim` fields. Provenance: `GenerationMode` enum + required `search_cardinality≥1` (prices the implicit search for selection-aware correction) + `preregistration_hash` (anti-HARKing hash-lock); `agent_generated⇒agent_id` validator. Governance: `HazardClass`/`AccessScope` enums (claim-level) + `blocks_reproduction` (→ dormant `unreproducible_by_governance` status) + `requires_safety_review` (→ SAFETY-GATE) helpers — grammar represents, protocol decides. Absorbs the largest re-ingest homeless cluster (premises + provenance metadata). Merge `738acfc`. Spec `specs/2026-06-02-provenance-governance-spec.md`, plan `plans/2026-06-02-provenance-governance.md`.
- ✅ Phase 7 (subject slot) — polymorphic **`Claim.subject`** (`subject.py`). 10-variant discriminated union (genomic_region, variant_vrs, s4_object, phenopacket, ontology_term, gene_or_protein, pathway, cohort, literal, composite) faithful to v1.2's SubjectRef, adapted to v1.3 frozen+hashable+forbid (no `dict` fields: canonical_allele dropped, phenopacket inline→JSON string, cohort predicates→`tuple[str,...]`, source_dataset.extra dropped; lists→tuples; literal structured→tuple-of-pairs). Mirrors `leaf.Leaf`; recursive `composite` via `model_rebuild()`. Additive-optional `Claim.subject: Subject | None = None` (back-compat). Closes the 47/47-homeless ingestion gap. Merge `eecf318`. Spec `specs/2026-06-02-subject-slot-spec.md`, plan `plans/2026-06-02-subject-slot.md`.
- ✅ Phase 8 — **the evaluator** (`operations.py` IR + `evaluate.py` runtime). The compiler/runtime split made real: a typed compute-graph IR (`DataHandle`/`NodeRef`/`OperationNode` w/ unbound `oracle_ref` slot → `ComputeGraph` acyclic/unique/resolvable + topo + content_hash → `SatisfactionCriterion` → `EvaluationPlan`), wired as additive-optional `Claim.evaluation_plan`. Runtime: pure adapter-injected `evaluate()` (topo exec, typed-leaf wrap, 3-valued criterion, drift, dimension-equality check; NEVER raises on a node/wrap error) + the **air-gapped `verify()` gate** (≥2 DISTINCT adapter identities or `SelfLicensingError`; mints an L2 `Satisfaction` ONLY on cross-adapter agreement [value-within-tol + verdict] AND SATISFIED — closes the L2 loop, no self-licensing; never assembles Licensing/Status). Two deterministic reference adapters (`IdentityAdapter` sum/len vs `ReferenceAdapter` `statistics.fmean`) give a genuine two-implementation check. Scope fence held: NO real network/R/scipy adapters, NO oracle dossier (slot only), NO full UCUM (equality only), NO Licensing/Status assembly. Merge `<fill>`. Spec `specs/2026-06-02-evaluator-spec.md`, plan `plans/2026-06-02-evaluator.md`. **Open follow-up:** `_check_agreement` compares value+verdict, not `terminal.dimension` (safe today since dimension is driven by the shared `produces` spec — revisit when real adapters land).
- ✅ Phase 6 — L4: AGM/TMS belief revision (`revision.py`). Belief-BASE AGM with `Cn` = L1 `entails`-neighborhood closure, inconsistency = `incompatible_with`. **Partial** entrenchment from StrengthVector (severity, evidence_against_null) + Status tier (`INCOMPARABLE` first-class). `restore_consistency` = Hansson consolidation: entrenchment-guided incision, robust/underdetermined spread + conservative consistent core (the locus of partial-entrenchment ambiguity). `expand`/`contract`/`revise` (Levi identity, success-privileged). **Edge hygiene**: every retracting op drops authored defeat edges incident to a retracted claim (no zombie-attack via grounded_extension's endpoint injection). Status recompute reuses Phase-5 `grounded_extension`. AGM postulates (success/inclusion/vacuity/consistency/extensionality) tested; base-contraction non-recovery documented, not faked. Merge `55096fd`. Spec `specs/2026-06-01-L4-revision-spec.md`, plan `plans/2026-06-01-L4-revision.md`.

## ▶ NEXT: #5 the 3 daemons (DRIFT / ORACLE-VALIDATION / REPRESENTATION RED-TEAM) + loop-economics · grammar `representation_revision` meta-tier

**The protocol runtime arc has STARTED.** Sub-projects #1, #2, #3a, #3b, #4a and #4b-slice-1 are DONE (see Current state). The 5-sub-project decomposition:
1. **Corpus + the assessment spine** ✅ DONE (merged no-ff to main, commit `c8b7279`).
2. **Oracle dossier** ✅ DONE (merged no-ff to main, commit `a61d7dd`).
3. **SELECT** — the pursuit/value engine. **#3a structural core** ✅ DONE (branch `feat/select-pursuit-engine-3a`, merge `03ae863`). **#3b QD / heterodox / Goodhart / accumulating belief** ✅ DONE (branch `feat/select-qd-belief-3b`, merge `4293faf`).
4. **GENERATE** — the proposer bus. **✅ COMPLETE.** #4a pure core (`5d7899f`); #4b-1 provisional links (`64b8042`); #4b-2 executable rivals + credit economy (`8e0bba0`); #4b-3 intelligent-operator seam (`feat/intelligent-operator-seam-4b`, merge `7c7a953`). The seam is the boundary for injected LLM/embedding operators; real model adapters + executable frontier-attack defense live behind it (deploy-time / future).
5. The 3 daemons (DRIFT, ORACLE-VALIDATION, REPRESENTATION RED-TEAM) + loop-economics. **#5a DRIFT ✅ DONE** (`ce107b9`); **#5b ORACLE-VALIDATION + F2 fix ✅ DONE** (`ea517c7`); **grammar `representation_revision` meta-tier ✅ DONE — Phase 7 COMPLETE** (`385c45e`); **#5c REPRESENTATION RED-TEAM ✅ DONE — all 3 daemons complete** (`9996e49`); **#5d loop-economics ✅ DONE — PROTOCOL RUNTIME COMPLETE** (`7e1d5c9`). All 5 sub-projects done.

**▶ STATE (end of autonomous-overnight 2026-06-05): the 5-sub-project protocol runtime is COMPLETE (`7e1d5c9`) and the overnight queue is DONE.** Completed overnight (all merged, local-only): #5d loop-economics (`7e1d5c9`); hygiene — shared `is_locked` + read-only `by_id` (`05bde78`); design specs for the `pip install` packaging harness + the 3D topology viewer (`0e60502`); a **4-auditor final code audit + live smokescreen** → **zero CRITICAL**, fixes merged (`bfdb0e6`): scheduler `_selectable` now reuses `select._is_candidate` (a hazard-gated-only corpus no longer livelocks the driver — the one IMPORTANT finding), DRIFT signal counts only claims that ACTUALLY drift, a BH-bar defense-in-depth guard, isolation test now guards grammar→protocol, + test hardening. **565 tests green; the live smokescreen (full cycle + 3 daemons + scheduler loop) passes 8/8.**

**▶ REMAINING (2026-06-05, post-semantics + post-security-audit):** ✅ RESOLVED this session — the `strength.py` uncertainty-polarity inconsistency (→ certainty rename `38f829e`); the VERIFY-vs-INTEGRATE credit-timing question (→ credit-on-survival `4fe6791`); F4 (was already wired). **Next user-greenlit work (autonomous-OK, build LOCAL only):** (1) the **3D viewer** `TopologyExport` slice (spec `…-topology-viewer-design.md`; rendering owned by a separate viewer instance, scale-gated); (2) **build the `pip install polymer-claims` harness** (spec `…-packaging-harness-design.md`; LOCAL build is fine — **publish stays blocked by the flagged account, user-gated**). **Tracked security/perf follow-ups (deferred by user's blocking-only scope):** M1 (canonicalize LICENSED-equivalence status), I2 (grounded_extension worklist rewrite + ingestion bounds — adversarial-corpus DoS), I1 defense-in-depth cardinality cap. Plus the audit's minor test-coverage suggestions + the credit-audit Goodhart ratification note (an operator producing license-then-retracted claims is invisible rather than penalized — consistent with "credit on survival", left as-is).

**(superseded) ▶ prior NEXT ACTION: brainstorm #5d loop-economics (the FINAL slice of #5 — and of the protocol-runtime arc).** ✅ DONE (`7e1d5c9`). **#5a DRIFT (`ce107b9`) + #5b ORACLE-VALIDATION (`ea517c7`) + grammar `representation_revision` meta-tier (`385c45e`, Phase 7 COMPLETE) + #5c RED-TEAM (`9996e49`) are DONE; ALL 3 DAEMONS COMPLETE; latent F2 is FIXED.** #5 is decomposed in the roadmap **`docs/superpowers/roadmaps/2026-06-04-sub5-daemons-roadmap.md`** (read it first — the #5d section). Start with `superpowers:brainstorming` on **#5d loop-economics**: a **pure scheduler** `next_action(state, *, budget) -> Action` that, given the corpus + ledgers + a passed-in compute budget, decides whether to run a `run_cycle` pass or which daemon pass (DRIFT / ORACLE-VALIDATION / RED-TEAM) to run next, and returns the chosen action for the caller to execute (purity: recommend-only, the caller loops + executes — NOT a `run_until` driver that owns the loop). Seam = #3a's `CostModel`/cost vector + #3b/#4b-2's `SelectionLedger` credit. Forks (from roadmap): (E1) round-robin vs value-ranked scheduling (reuse the EIG/credit machinery to prioritize the highest-expected-value pass); (E2) recommend-only vs own-the-loop — **recommend recommend-only** (purity); (E3) per-daemon budget shares vs a single shared budget the scheduler allocates. **Purity stance for ALL #5 (decided): a daemon/scheduler is a pure transform, caller-scheduled — NO threads/clock/Date/random; everything time-like is passed in.** State threads like `SelectionLedger`, NOT a 5th Corpus collection. Same rhythm as #1–#5c: brainstorm → spec → plan → subagent-driven → merge no-ff → memory. **After #5d, the 5-sub-project protocol runtime is COMPLETE** — then the deferred 3D topology viewer + the `pip install polymer-claims` harness ([[project_polymer_claims_platform_vision]]).

**Open fronts AFTER #4b-provisional (then `superpowers:brainstorming`):** **#4b executable-generation / embedding-LLM operators** (the rest of GENERATE — needs an embedding/benchmark substrate); **#5 daemons** (DRIFT / ORACLE-VALIDATION / REPRESENTATION RED-TEAM + loop-economics — the `SelectionLedger` surprise-Goodhart signal gives them a home); **grammar `representation_revision` meta-tier** (last Phase-7 item; unblocks operator-5's representation-revision lane). The audit's latent finding **F2 was FIXED** in #5b. **The broader `dominates`/`licensed` uncertainty-polarity inconsistency in `strength.py` is now ALSO RESOLVED** (2026-06-05 certainty rename `38f829e` — the axis is `certainty`, uniformly higher-is-better; the F2 oracle special-case collapsed to a plain `meet`).
Keep spine invariants whichever is chosen: pure/deterministic, one-way isolation, Corpus at 4 collections, no LLM/embeddings in the core. SELECT scoping note (now #3a-historical): `docs/superpowers/specs/2026-06-03-select-scoping-notes.md`.

**Sub-project #1 design (approved, in the spec):** new sibling uv pkg `protocol/` → import pkg `polymer_protocol`, one-way dep on `polymer_grammar` (isolation-tested); **writes ONLY grammar IR** (no new grammar fields, no per-claim wrapper — stage outputs map onto existing fields: equivalence_class→`equivalences` tuple, hazard gate→`governance` predicate, hash-lock→`provenance.preregistration_hash`, status/license→`Claim.status`/`Licensing`, FDR→`fdr_ledger`). `Corpus` = (claims, defeat_edges, equivalences, fdr_ledger), frozen. 7 pure stages `represent/canonicalize/safety_gate/commit/execute_ground/verify_stage/integrate` + `run_cycle`; EXECUTE reuses Phase-8 `verify()` (air-gap); GENERATE+SELECT are stubbed open ports (claims injected exogenously, execute-all-committed). End goal: `pip install polymer-claims` (grammar+protocol+CLI bundled later) → local node → federated claims universe ([[platform vision]]). Deep design source: `~/Desktop/Research/topics/epistemic-claim-foundations/generative-protocol/_FINAL_knowledge_generation_protocol.md`.

- Protocol frontier test: add an integration-depth test where INTEGRATE changes the defeat graph (derived rebut edges / retraction) so the post-INTEGRATE frontier genuinely differs from the head scaffolding — pins that run_cycle emits the tail represent() frontier, not the head. (Needs a conclusion-bearing multi-claim fixture; plan-deferred to integration tests.)
- Protocol follow-ups surfaced by the whole-package review (own them in #3 SELECT): (a) the emitted `frontier` is status-blind — a REJECTED claim can still appear (its attacker's rejection doesn't strip the edge); a downstream consumer must not assume `frontier ⊆ live claims` until the calibrated-posterior/strength feedback lands. (b) the "locked" predicate (`provenance.preregistration_hash` present) is expressed independently in `execute._is_locked/_is_executable`, `commit._is_locked`, and `cycle._locked_ids` — consolidate into one shared helper before commit semantics grow. (c) `Corpus.by_id()` returns a mutable `dict` (cosmetic vs the tuples-only/frozen discipline).
- **Smoke/validity audit 2026-06-02 (8 live `run_cycle` scenarios)** — spine sound (air-gap holds end-to-end incl. same-identity; determinism byte-identical ×5; multi-cycle stable; AGM contest restores consistency; writes-only-IR confirmed). **FIXED on `main` (`78af588`): canonicalize collapsed claims on a degenerate all-absent key** (bare same-pattern skeletons → false `a==b`, even across a defeat pair) — added `_has_discriminating_content` guard. **Still-open #3 follow-ups filed by the audit:** (d) **LICENSED ⊬ FDR-discovery** — `verify_stage` decides licensing without consulting `fdr_ledger`, so a claim can be LICENSED while the corpus error budget doesn't count it a discovery; the selection-aware/cardinality-scaled threshold (#3) must reconcile. (e) **`commit` hash-locks safety-gated claims** (COMMIT runs after SAFETY and doesn't re-check the gate; harmless — EXECUTE re-checks and skips — but a human-review-lane claim gets autonomously pre-registered; have commit skip gated claims). (f) **equivalence-consumption caveat for #3:** two claims with identical content but different governance still (correctly) collapse to one equivalence class — when #3 starts CONSUMING `equivalences` (semantic dedup / propagating status across a class), it MUST reconcile governance (take the stricter posture) and must NOT propagate LICENSED across a class without re-gating, or a hazardous claim could inherit licensing from a public twin.

Phase-8 carry-forwards (none blocking): real network/R/scipy adapters outside the package; full UCUM dimensional algebra (only equality now); `_check_agreement` dimension-awareness; vector-valued `ExecValue`/`Leaf`; then the deferred 3D topology viewer.

**Carry-forward follow-ups (open):** a faithful subject-carrying v1.2→v1.3 re-ingest (now possible — the slot exists); a real cohort predicate algebra (currently prose `tuple[str,...]`); vector-valued `Leaf` (the remaining ingestion gap); auxiliary assumptions as first-class blame/undercut/retraction nodes (all 47 v1.2 claims carry `external_assumptions`); n-ary `incompatible_with` + conjunctive `entails` (would make L4 contraction's entrenchment choice bite); bounded defeat in-degree cap; named-audience VAF value-orderings; per-pattern stratified FDR + PPV-floor (the other two parts of §4); LORD++ upgrade. Same rhythm always: brainstorm forks → phase spec → plan → subagent-driven.

## How to resume (the established rhythm)

1. Read memory **`project_polymer_claims_knowledge_protocol`** (full phase history + follow-ups).
2. Read the **unified spec**: `docs/superpowers/specs/2026-05-31-unified-claim-foundations-spec.md`
   (+ the HTML schema overview / spatial map alongside it).
3. Brainstorm only the genuine forks (2–3 questions max), write a **phase spec** →
   `docs/superpowers/specs/`, then a **plan** (writing-plans skill) → `docs/superpowers/plans/`.
4. Execute **subagent-driven** (superpowers:subagent-driven-development): fresh implementer +
   spec-compliance review + code-quality review per task; final whole-package review (Opus);
   then merge to `main` (no-ff), verify tests on merged result, delete the branch.
5. **Update the plan's Progress Log after every task**, and this file + memory at phase end.

## Invariants / working agreements (don't relearn these)

- `grammar/` must NEVER import `polymer_formalclaim` — enforced by `grammar/tests/test_isolation.py`.
- All models subclass `_Model` (frozen, `extra="forbid"`). **Collections are tuples** (deep
  immutability + hashability for content-addressing). No `dict`/`list` fields on models.
- New cross-cutting fields land **additive/optional** (`X | None = None`) with a
  *present-only-when-Y* validator, mirroring `conclusion`/`licensing`/`roles`. Hard "required"
  gates are deferred to a later tightening phase.
- Tests: `cd grammar && uv run pytest -q` (+ `uv run ruff check src tests`). TDD: failing test first.
- Momentum over exhaustive questions; merge to `main` locally (commits are NOT pushed to origin).
- Subagent connection-drops happen — work is usually already committed; verify + finish controller-side.

## Open follow-ups (tracked, none blocking)

- Reconcile `CausalRoles.adjustment_set` with `Pattern.adjustment_role` (free-str placeholder).
- Hard `LICENSED ⇒ licensing` + `conclusion required` tightening phase (will flip back-compat tests).
- UCUM↔Dimension parsing + evaluator dimensional enforcement (Phase 8).
- Optional `min_length=1` guards on role variable names + dimension base names.
- `.pytest_cache`/`.ruff_cache` → root `.gitignore`; document extended `MeasurementBasis`.
- **Ontology (carry-forward, user-flagged important for v1.3):** the small-IR + versioned ontology-bound *domain profile* idea (v1.2 note now at `v1.2/docs/FormalClaim_Domain_Ontology_Note.md`) is load-bearing for v1.3 — absorbed into the unified spec §3.1 (profiles = ontology slot-legality) + §7 (functorial ontology migration). Keep it live when patterns/profiles get built; don't treat it as frozen with v1.2.
- **v1.2 ingestion test (user: "a must") — DONE 2026-06-01.** Probe `grammar/scripts/probe_v12_ingest.py`; findings `docs/superpowers/specs/2026-06-01-v12-ingestion-findings.md`. 47/47 claims build a Claim *skeleton* but only via a PropositionLeaf fallback + fabricated pattern. **Confirmed Phase 5 is well-motivated** (all 47 carry external_assumptions + depends_on). **Two OFF-ROADMAP gaps surfaced:** (1) no `Claim.subject` slot (biggest fidelity hole), (2) vector-valued statistics have no single-`Leaf` home (~29 in corpus). Pattern-inference (raw claim → registry pattern) unsolved. Decide sequencing of these vs Phase 5 next.

## Remaining phases after L3

6 — L4 AGM/TMS revision · 7 — protocol-imposed fields (generated_by, oracle credibility,
hazard/governance, online-FDR, representation_revision tier) · 8 — the evaluator. Then the
**protocol runtime** (8-stage flywheel + 3 daemons), then the deferred **3D topology viewer**.
